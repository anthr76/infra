---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: ostree-repo
  namespace: infra
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 150Gi
  storageClassName: ceph-filesystem

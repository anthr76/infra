---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: hajimari-config-v2
  namespace: infra
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: fast-ceph-filesystem

---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: calibre-web-config-v2
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Gi
  storageClassName: fast-ceph-filesystem

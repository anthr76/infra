---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seeer-config
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: fast-ceph-block

---
apiVersion: batch/v1
kind: CronJob
metadata:
  name: &app recyclarr
  namespace: media
spec:
  schedule: "0 3 * * *"
  concurrencyPolicy: "Forbid"
  successfulJobsHistoryLimit: 3
  failedJobsHistoryLimit: 5
  jobTemplate:
    spec:
      backoffLimit: 3
      ttlSecondsAfterFinished: 300
      template:
        spec:
          automountServiceAccountToken: false
          restartPolicy: OnFailure
          containers:
            - name: sonarrs
              image: ghcr.io/k8s-at-home/recyclarr:v2.2.1@sha256:72a605a8deeb4527bd8d03553677a5aae018cca6e800c2dfb7f299cfdc3fbe8e
              env:
                - name: TZ
                  value: America/New_York
              command:
                - /app/recyclarr
              args:
                - sonarr
                - --app-data
                - /tmp/recyclarr                
                - --config
                - /config/recyclarr.yaml
              volumeMounts:
                - name: config
                  mountPath: /config
            - name: radarrs
              image: ghcr.io/k8s-at-home/recyclarr:v2.2.1@sha256:72a605a8deeb4527bd8d03553677a5aae018cca6e800c2dfb7f299cfdc3fbe8e
              env:
                - name: TZ
                  value: "America/New_York"
              command:
                - /app/recyclarr
              args:
                - radarr
                - --app-data
                - /tmp/recyclarr                  
                - --config
                - /config/recyclarr.yaml
              volumeMounts:
                - name: config
                  mountPath: /config/
          volumes:
            - name: config
              secret:
                secretName: *app

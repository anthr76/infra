---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: jellyfin-config-v2
  namespace: media
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 15Gi
  storageClassName: fast-ceph-filesystem

---
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: cilium
  namespace: kube-system
spec:
  addresses:
  - type: IPAddress
    value: 10.45.0.80
  gatewayClassName: cilium
  listeners:
  - allowedRoutes:
      namespaces:
        from: All
    name: web-gw
    port: 80
    protocol: HTTP

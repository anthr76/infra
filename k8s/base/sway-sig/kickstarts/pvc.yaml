---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tekton-ostree-workspace
  namespace: sway-sig
spec:
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 35Gi
  accessModes:
    - ReadWriteMany

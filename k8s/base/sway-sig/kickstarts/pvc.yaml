---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tekton-kickstart-workspace
  namespace: sway-sig
spec:
  storageClassName: ceph-filesystem
  resources:
    requests:
      storage: 10Gi
  accessModes:
    - ReadWriteMany

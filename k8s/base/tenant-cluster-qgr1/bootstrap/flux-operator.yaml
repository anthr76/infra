---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tenant-cluster-flux-operator
  namespace: tenant-cluster-qgr1
spec:
  kubeConfig:
    secretRef:
      name: qgr1-admin-kubeconfig
      key: admin.conf
  interval: 10m0s
  path: ./k8s/base/flux-system/flux-operator
  prune: true
  retryInterval: 1m
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
  timeout: 5m
  wait: false
  patches:
    - patch: |-
        apiVersion: helm.toolkit.fluxcd.io/v2
        kind: HelmRelease
        metadata:
          name: flux-operator
          namespace: flux-system
        spec:
          kubeConfig:
            secretRef:
              name: qgr1-admin-kubeconfig
              key: admin.conf
      target:
        kind: HelmRelease
        name: flux-operator
        namespace: flux-system

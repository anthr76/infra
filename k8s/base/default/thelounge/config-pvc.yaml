---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: thelounge
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: fast-ceph-block

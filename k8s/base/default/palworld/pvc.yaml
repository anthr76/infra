---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
  name: palworld-server
  namespace: default
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 12Gi
  storageClassName: fast-ceph-block

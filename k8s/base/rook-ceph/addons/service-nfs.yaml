---
apiVersion: v1
kind: Service
metadata:
  name: rook-ceph-nfs
  namespace: rook-ceph
  annotations:
    metallb.universe.tf/loadBalancerIPs: 10.45.0.10
spec:
  ports:
    - name: nfs
      port: 2049
  type: LoadBalancer
  externalTrafficPolicy: Local
  selector:
    app: rook-ceph-nfs
    ceph_nfs: nfs
    instance: a

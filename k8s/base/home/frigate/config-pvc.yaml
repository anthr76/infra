---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: frigate-config-v3
  namespace: home
spec:
  storageClassName: fast-ceph-filesystem
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 5Gi

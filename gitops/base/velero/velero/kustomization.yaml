apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
namespace: velero
generatorOptions:
  disableNameSuffixHash: true

secretGenerator:
  - name: s3-credentials
    files:
      - cloud=s3-credentials.sops

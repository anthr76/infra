---
apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: daily-backup-snapshot
  namespace: velero
spec:
  schedule: 0 12 * * *
  template:
    includedNamespaces:
      - "*"
    excludedNamespaces:
      - "crypto"
    snapshotVolumes: true
      - default
    ttl: "120h"

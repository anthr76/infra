---
apiVersion: velero.io/v1
kind: BackupStorageLocation
metadata:
  name: default
  namespace: velero
spec:
  provider: velero.io/aws
  objectStorage:
    bucket: velero
    prefix: ${CLUSTER_NAME}
  config:
    region: us-east-1
    s3ForcePathStyle: "true"
    publicUrl: "https://s3.kutara.io"
    s3Url: "https://s3.kutara.io"
    profile: "default"
  credential:
    name: s3-credentials
    key: cloud

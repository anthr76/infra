---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
namespace: rook-ceph
generatorOptions:
  disableNameSuffixHash: true
secretGenerator:
  - name: democratic-csi
    files:
      - driver-config-file.yaml=secret.sops.yaml

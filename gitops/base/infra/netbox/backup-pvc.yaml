---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: netbox-db-backup
  namespace: infra
spec:
  storageClassName: "zfs-nfs"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 15Gi

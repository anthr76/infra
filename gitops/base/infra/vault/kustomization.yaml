---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
  - secret.sops.yaml
namespace: infra
secretGenerator:
  - name: kms-creds
    files:
      - credentials.json=gcp-credentials.sops.json

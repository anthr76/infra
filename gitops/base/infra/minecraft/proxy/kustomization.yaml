---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - helm-release.yaml
namespace: infra
secretGenerator:
  - name: minecraft-luckperms-proxy
    files:
      - config.yml=luckperms-config.sops.yml
generatorOptions:
  disableNameSuffixHash: true
commonLabels:
  kustomize.toolkit.fluxcd.io/substitute: "disabled"

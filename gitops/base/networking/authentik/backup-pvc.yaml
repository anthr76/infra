---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: authentik-db-backup
  namespace: networking
spec:
  storageClassName: "zfs-nfs"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: error-pages
  name: error-pages
  namespace: networking
spec:
  ports:
    - name: http
      port: 8080
      targetPort: http
  selector:
    app.kubernetes.io/instance: error-pages
    app.kubernetes.io/name: error-pages

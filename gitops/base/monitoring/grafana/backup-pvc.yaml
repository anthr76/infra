---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: grafana-db-backup
  namespace: monitoring
spec:
  storageClassName: "zfs-nfs"
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi

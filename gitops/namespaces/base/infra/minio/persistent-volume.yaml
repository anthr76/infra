---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: minio-nfs
  namespace: infra
spec:
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  nfs:
    server: "${NAS_1_ADDRESS}"
    path: "/tank/minio"

---
apiVersion: v1
kind: PersistentVolume
metadata:
  name: minio-nfs
  namespace: infra
spec:
  capacity:
    storage: 1Mi
  accessModes:
    - ReadWriteMany
  mountOptions:
    - nfsvers=4.1
  nfs:
    server: "${NAS_1_ADDRESS}"
    path: "/tank/minio"

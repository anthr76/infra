---
apiVersion: notification.toolkit.fluxcd.io/v1beta1
kind: Receiver
metadata:
  name: gitlab-receiver
  namespace: flux-system
spec:
  type: gitlab
  events:
    - "Push Hook"
  secretRef:
    name: gitlab-webhook-token
  resources:
    - apiVersion: source.toolkit.fluxcd.io/v1beta1
      kind: GitRepository
      name: 'flux-system'
      namespace: 'flux-system'
    - apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
      kind: Kustomization
      name: 'flux-system'
      namespace: 'flux-system'
    # - apiVersion: helmcharts.source.toolkit.fluxcd.io
    #   kind: HelmChart
    #   name: '*'
    #   namespace: 'flux-system'
    # - apiVersion: helmreleases.helm.toolkit.fluxcd.io
    #   kind: HelmRelease
    #   name: '*'
    #   namespace: '*'

---
apiVersion: traefik.containo.us/v1alpha1
kind: Middleware
metadata:
  name: drop-cloudflare-cdn
  namespace: networking
spec:
  headers:
    customResponseHeaders:
      contentSecurityPolicy: "" # Removes

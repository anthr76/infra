apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: longhorningressroute
  namespace: longhorn-system

spec:
  entryPoints:
    - web

  routes:
  - match: Host(`longhorn.k8skvm.rabbito.tech`)
    kind: Rule
    services:
    - name: longhorn
      port: 80

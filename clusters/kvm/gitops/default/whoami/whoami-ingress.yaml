apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: myingressroute
  namespace: default

spec:
  entryPoints:
    - web

  routes:
  - match: Host(`whoami.k3s`) || Host(`whoami.k3s.rabbito.tech`) || Host(`whoami`)
    kind: Rule
    services:
    - name: whoami
      port: 80

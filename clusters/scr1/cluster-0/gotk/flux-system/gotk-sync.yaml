---
apiVersion: source.toolkit.fluxcd.io/v1beta1
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/anthr76/infra
  ignore: |
    # exclude all
    /*
    # include gitops dirs
    !/clusters
    !/gitops
    # exclude text files from gitops dirs
    /clusters/**/*.md
    /clusters/**/*.txt
    /gitops/**/*.md
    /gitops/**/*.txt
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta1
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/scr1/cluster-0/gotk
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  validation: client

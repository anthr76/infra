---
# Kubelt

kubernetes_control_plane_endpoint:
  address: 'nyc1-lb-1.k8s.rabbito.tech'
  port: 6443

kubernetes_pod_network:
  cidr: '10.244.0.0/16'

kubernetes_kubeadm_init_extra_opts: "--skip-phases=addon/kube-proxy --ignore-preflight-errors=NumCPU --ignore-preflight-errors=Mem"

# CNI
cni_plugin: "cilium"
